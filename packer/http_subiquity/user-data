#cloud-config
autoinstall:
  version: 1
  user-data:
    hostname: ubuntu2004
    users:
      - name: installer
        passwd: $6$kpUd9wTHa3xt.2Bz$EawasoHiv3Z8CD5l63dG2NnhuIorC0pCwznj0Gpt5m4jb4Z0iUJn26lR7BKaGAAtM6sqE9SfQ4jqzYUlZsXdX/
        lock-passwd: false
        groups: sudo
        shell: /bin/bash
        sudo: "ALL=(ALL) NOPASSWD:ALL"
    locale: en_US
  ssh:
    allow-pw: true
    install-server: true
  keyboard: 
    layout: gb
  storage:
    layout:
      name: direct
    config:
      - type: disk
        id: disk0
        match:
          size: largest
      - type: partition
        id: boot-partition
        device: disk0
        size: 500M
      - type: partition
        id: root-partition
        device: disk0
        size: -1
  late-commands:
    - "echo 'Defaults:installer !requiretty' > /target/etc/sudoers.d/installer"
    - "echo 'installer ALL=(ALL) NOPASSWD: ALL' >> /target/etc/sudoers.d/installer"
    - "chmod 440 /target/etc/sudoers.d/installer"

    